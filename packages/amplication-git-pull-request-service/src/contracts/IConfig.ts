export interface IConfig {
  /**
   * This param give as the base folder that the from that we can get to the builds folders by the path [baseBuildsPath]/builds/[app-id]/[build-id]
   */
  baseBuildsPath: string;
}

/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { PackageFileService } from "../packageFile.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PackageFileCreateInput } from "./PackageFileCreateInput";
import { PackageFile } from "./PackageFile";
import { PackageFileFindManyArgs } from "./PackageFileFindManyArgs";
import { PackageFileWhereUniqueInput } from "./PackageFileWhereUniqueInput";
import { PackageFileUpdateInput } from "./PackageFileUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class PackageFileControllerBase {
  constructor(
    protected readonly service: PackageFileService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PackageFile })
  @nestAccessControl.UseRoles({
    resource: "PackageFile",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createPackageFile(
    @common.Body() data: PackageFileCreateInput
  ): Promise<PackageFile> {
    return await this.service.createPackageFile({
      data: {
        ...data,

        packageField: data.packageField
          ? {
              connect: data.packageField,
            }
          : undefined,
      },
      select: {
        createdAt: true,
        diff: true,
        fullContent: true,
        id: true,
        originalFileChecksum: true,

        packageField: {
          select: {
            id: true,
          },
        },

        path: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [PackageFile] })
  @ApiNestedQuery(PackageFileFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PackageFile",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async packageFiles(@common.Req() request: Request): Promise<PackageFile[]> {
    const args = plainToClass(PackageFileFindManyArgs, request.query);
    return this.service.packageFiles({
      ...args,
      select: {
        createdAt: true,
        diff: true,
        fullContent: true,
        id: true,
        originalFileChecksum: true,

        packageField: {
          select: {
            id: true,
          },
        },

        path: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PackageFile })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PackageFile",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async packageFile(
    @common.Param() params: PackageFileWhereUniqueInput
  ): Promise<PackageFile | null> {
    const result = await this.service.packageFile({
      where: params,
      select: {
        createdAt: true,
        diff: true,
        fullContent: true,
        id: true,
        originalFileChecksum: true,

        packageField: {
          select: {
            id: true,
          },
        },

        path: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PackageFile })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PackageFile",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updatePackageFile(
    @common.Param() params: PackageFileWhereUniqueInput,
    @common.Body() data: PackageFileUpdateInput
  ): Promise<PackageFile | null> {
    try {
      return await this.service.updatePackageFile({
        where: params,
        data: {
          ...data,

          packageField: data.packageField
            ? {
                connect: data.packageField,
              }
            : undefined,
        },
        select: {
          createdAt: true,
          diff: true,
          fullContent: true,
          id: true,
          originalFileChecksum: true,

          packageField: {
            select: {
              id: true,
            },
          },

          path: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PackageFile })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PackageFile",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deletePackageFile(
    @common.Param() params: PackageFileWhereUniqueInput
  ): Promise<PackageFile | null> {
    try {
      return await this.service.deletePackageFile({
        where: params,
        select: {
          createdAt: true,
          diff: true,
          fullContent: true,
          id: true,
          originalFileChecksum: true,

          packageField: {
            select: {
              id: true,
            },
          },

          path: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}

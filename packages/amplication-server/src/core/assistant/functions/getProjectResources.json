{
  "name": "getProjectResources",
  "description": "get a list of resources in a project",
  "parameters": {
    "type": "object",
    "properties": {
      "projectId": {
        "type": "string",
        "description": "The ID of the project"
      }
    },
    "required": ["projectId"]
  }
}

{
  "name": "getServiceModules",
  "description": "get a list of modules in a service",
  "parameters": {
    "type": "object",
    "properties": {
      "serviceId": {
        "type": "string",
        "description": "The ID of the service"
      }
    },
    "required": ["serviceId"]
  }
}

{
  "name": "createBlueprint",
  "description": "Create a blueprint.",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "The name of the blueprint"
      }
    },
    "required": ["name"]
  }
}

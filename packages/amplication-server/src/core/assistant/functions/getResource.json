{
  "name": "getResource",
  "description": "get the details of a specific resource",
  "parameters": {
    "type": "object",
    "properties": {
      "resourceId": {
        "type": "string",
        "description": "The ID of the resource"
      }
    },
    "required": ["resourceId"]
  }
}

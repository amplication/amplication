{
  "name": "createModule",
  "description": "creates a new module in a resource. ",
  "parameters": {
    "type": "object",
    "properties": {
      "moduleName": {
        "type": "string",
        "description": "the name of the module. for example: System, Customer, Order, Util."
      },
      "moduleDescription": {
        "type": "string",
        "description": "the description of the module."
      },
      "resourceId": {
        "type": "string",
        "description": "the ID of the resource in which the module is created. If there are multiple resources available in the context, show the user a list of resources to choose from. If there is no resource available, do not use projectId instead"
      }
    },
    "required": ["moduleName", "resourceId", "moduleDescription"]
  }
}

{
  "name": "commitProjectPendingChanges",
  "description": "Commit all the pending changes, and build the generated code for all resources in the project",
  "parameters": {
    "type": "object",
    "properties": {
      "projectId": {
        "type": "string",
        "description": "the ID of the project"
      },
      "commitMessage": {
        "type": "string",
        "description": "a message that describes the changes in the commit. The message can be created based on the pending changes in the project"
      }
    },
    "required": ["projectId", "commitMessage"]
  }
}

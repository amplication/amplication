# Migration `20200826133742`

This migration has been generated by Iddan Aaronsohn at 8/26/2020, 4:37:42 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."EntityPermissionFieldRole" DROP CONSTRAINT "EntityPermissionFieldRole_entityPermissionFieldId_fkey"

ALTER TABLE "public"."EntityPermissionFieldRole" DROP CONSTRAINT "EntityPermissionFieldRole_entityPermissionRoleId_fkey"

CREATE TABLE "public"."_EntityPermissionFieldToEntityPermissionRole" (
"A" text  NOT NULL ,
"B" text  NOT NULL )

CREATE UNIQUE INDEX "_EntityPermissionFieldToEntityPermissionRole_AB_unique" ON "public"."_EntityPermissionFieldToEntityPermissionRole"("A","B")

CREATE  INDEX "_EntityPermissionFieldToEntityPermissionRole_B_index" ON "public"."_EntityPermissionFieldToEntityPermissionRole"("B")

ALTER TABLE "public"."_EntityPermissionFieldToEntityPermissionRole" ADD FOREIGN KEY ("A")REFERENCES "public"."EntityPermissionField"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_EntityPermissionFieldToEntityPermissionRole" ADD FOREIGN KEY ("B")REFERENCES "public"."EntityPermissionRole"("id") ON DELETE CASCADE ON UPDATE CASCADE

DROP TABLE "public"."EntityPermissionFieldRole";
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200826110936..20200826133742
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider        = "prisma-client-js"
@@ -182,39 +182,29 @@
   @@unique([entityVersionId, action])
 }
 model EntityPermissionRole {
-  id                   String                      @default(cuid()) @id
-  entityPermission     EntityPermission            @relation(fields: [entityPermissionId], references: [id])
+  id                   String                  @default(cuid()) @id
+  entityPermission     EntityPermission        @relation(fields: [entityPermissionId], references: [id])
   entityPermissionId   String
-  appRole              AppRole                     @relation(fields: [appRoleId], references: [id])
+  appRole              AppRole                 @relation(fields: [appRoleId], references: [id])
   appRoleId            String
-  permissionFieldRoles EntityPermissionFieldRole[]
+  permissionFieldRoles EntityPermissionField[]
   @@unique([entityPermissionId, appRoleId])
 }
 model EntityPermissionField {
-  id                   String                      @default(cuid()) @id
-  entityPermission     EntityPermission            @relation(fields: [entityPermissionId], references: [id])
+  id                   String                 @default(cuid()) @id
+  entityPermission     EntityPermission       @relation(fields: [entityPermissionId], references: [id])
   entityPermissionId   String
-  field                EntityField                 @relation(fields: [fieldId], references: [id])
+  field                EntityField            @relation(fields: [fieldId], references: [id])
   fieldId              String
-  permissionFieldRoles EntityPermissionFieldRole[]
+  permissionFieldRoles EntityPermissionRole[]
   @@unique([entityPermissionId, fieldId])
 }
-model EntityPermissionFieldRole {
-  id                      String                @default(cuid()) @id
-  entityPermissionField   EntityPermissionField @relation(fields: [entityPermissionFieldId], references: [id])
-  entityPermissionFieldId String
-  entityPermissionRole    EntityPermissionRole  @relation(fields: [entityPermissionRoleId], references: [id])
-  entityPermissionRoleId  String
-
-  @@unique([entityPermissionFieldId, entityPermissionRoleId])
-}
-
 model EntityField {
   id               String        @default(cuid()) @id
   createdAt        DateTime      @default(now())
   updatedAt        DateTime      @updatedAt
```



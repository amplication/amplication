# Migration `20200906141416`

This migration has been generated by Yuval Hazaz at 9/6/2020, 5:14:16 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Entity" DROP COLUMN "allowFeedback",
DROP COLUMN "isPersistent",
DROP COLUMN "primaryField"

ALTER TABLE "public"."EntityVersion" ADD COLUMN "name" text   NOT NULL ,
ADD COLUMN "displayName" text   NOT NULL ,
ADD COLUMN "pluralDisplayName" text   NOT NULL ,
ADD COLUMN "description" text   

ALTER INDEX "public"."EntityPermissionField.entityPermissionId_fieldPermanentId_uniqu" RENAME TO "EntityPermissionField.entityPermissionId_fieldPermanentId_unique"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200904112718..20200906141416
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider        = "prisma-client-js"
@@ -126,12 +126,8 @@
   name              String
   displayName       String
   pluralDisplayName String
   description       String?
-  isPersistent      Boolean
-  // TBC
-  allowFeedback     Boolean
-  primaryField      String?
   entityVersions    EntityVersion[]
   lockedByUser      User?           @relation(fields: [lockedByUserId], references: [id])
   lockedByUserId    String?
   lockedAt          DateTime?
@@ -148,8 +144,12 @@
   updatedAt         DateTime           @updatedAt
   entity            Entity             @relation(fields: [entityId], references: [id])
   entityId          String
   versionNumber     Int
+  name              String
+  displayName       String
+  pluralDisplayName String
+  description       String?
   entityFields      EntityField[]
   commit            Commit?            @relation(fields: [commitId], references: [id])
   commitId          String?
   entityPermissions EntityPermission[]
```



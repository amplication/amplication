// import axios from "axios";
import { LogEntry } from "winston";

export const createLog = async (log: LogEntry): Promise<void> => {
  // await axios.post(process.env.CREATE_LOG_URL || "", {
  //   buildId: process.env.BUILD_ID,
  //   ...log,
  // });
};

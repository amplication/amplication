import { builders } from "ast-types";

export const CLASS_TRANSFORMER_MODULE = "class-transformer";
export const TYPE_ID = builders.identifier("Type");
export const TRANSFORM_ID = builders.identifier("Transform");

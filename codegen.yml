schema: "apps/server/src/schema.graphql"
generates:
  apps/client/src/models.ts:
    plugins:
      - "typescript"
  apps/data-service-generator/src/models.ts:
    plugins:
      - "typescript"
  apps/git-pull-request-service/src/models.ts:
    plugins:
      - "typescript"
  libs/code-gen-types/src/models.ts:
    plugins:
      - "typescript"
  libs/cli/src/models.ts:
    plugins:
      - "typescript"
hooks:
  afterAllFileWrite:
    - prettier --write

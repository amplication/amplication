apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dsg-jobs
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: dsg-jobs
  storageClassName: efs-sc
  volumeMode: Filesystem

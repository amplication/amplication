name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  who-to-greet:  # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
outputs:
  service_hashes:
    description: 'service hashes'
runs:
  using: 'node16'
  main: 'lib/main.js'
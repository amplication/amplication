# Migration `20200608150347`

This migration has been generated by Iddan Aaronsohn at 6/8/2020, 3:03:47 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."Customer" (
"createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,"email" TEXT NOT NULL  ,"firstName" TEXT NOT NULL  ,"id" TEXT NOT NULL  ,"lastName" TEXT NOT NULL  ,"updatedAt" DATE NOT NULL  ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "quaint"."Customer.email" ON "Customer"("email")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200608150347
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,19 @@
+// Prisma SDK
+
+datasource sqlite {
+    provider = "sqlite"
+    url      = "file:./dev.db"
+}
+
+generator client {
+    provider = "prisma-client-js"
+}
+
+model Customer {
+    id        String   @default(cuid()) @id
+    createdAt DateTime @default(now())
+    updatedAt DateTime @updatedAt
+    email     String   @unique
+    firstName String
+    lastName  String
+}
```


